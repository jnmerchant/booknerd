<%= form_for(library) do |f| %>
  <% if library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(library.errors.count, "error") %> prohibited this library from being saved:</h2>

      <ul>
      <% library.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :library_type %>
    <%= f.text_field :library_type %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <!-- <%= f.text_field :user_id %> -->
    <%= f.collection_select :user_id, User.order(:name), :id, :name, include_blank: false %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
